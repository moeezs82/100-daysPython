import os
import shutil

cur_dir = os.path.dirname(os.path.realpath(__file__))

# shutil.copy(os.path.join(cur_dir, "folder/test.txt"), os.path.join(cur_dir, "test.txt"))

# shutil.copytree(os.path.join(cur_dir, "folder"), os.path.join(cur_dir, "folder copy"))

# shutil.move(os.path.join(cur_dir, "folder copy/test.txt"), os.path.join(cur_dir, "test.txt"))

# shutil.rmtree(os.path.join(cur_dir, "folder copy"))
